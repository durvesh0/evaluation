<!-- <?xml version="1.0" encoding="UTF-8"?>
enable use-expresssions
<http auto-config="true" use-expressions="true">
	<intercept-url pattern="/admin**" access="hasRole("ROLE_ADMIN") />
	Access Denied Page
	<access-denied-handler error-page="/403" />
	<form-login
		login-page="/login"
		default-target-url="/index"
		authantication-failure-url="/login?error"
		username-parameter="username"
		password-parameter="password" />
	<logout logout-success-url="/login?logout" />
	enable csrf protection
		<csrf />
</http>

 select users and user_roles from database  
<authantication-manager>
	<authantication-provider>
		<jdbc-user-service data-source-ref="dataSource"
			users-by-username-query=
				"select username,password enable from sc_users where username=?"
			authorities-by-username-query=
				"select username, role from sc_user_roles where username=?" />
	</authantication-provider>
</authantication-manager>	
		
 -->